# Hustlrzz backend dependencies — pinned for reproducible, supply-chain-safe
# installs. These versions ship prebuilt wheels for CPython 3.10 (Docker/Render)
# and 3.11 (CI). Bump deliberately and re-run the test suite.
#
# To update a package: bump the pin, then run `pytest` and rebuild.

# --- Web framework ---
fastapi==0.115.14
uvicorn[standard]==0.34.3
python-multipart==0.0.31  # CVE-2026-24486/40347/42561/53537-40 (7 Dependabot advisories)
pydantic[email]==2.13.4
email-validator==2.2.0

# --- Supabase (auth + Postgres) ---
supabase==2.31.0

# --- HTTP clients ---
httpx==0.28.1
requests==2.33.0  # CVE-2024-47081, CVE-2026-25645
PyJWT==2.13.0

# --- PDF resume parsing ---
PyMuPDF==1.25.3

# --- Web search & scraping ---
beautifulsoup4==4.13.3
lxml==6.1.0  # CVE-2026-41066 (XXE in iterparse/ETCompatXMLParser)
playwright==1.52.0
duckduckgo-search==8.1.1

# --- Misc ---
python-dotenv==1.2.2  # CVE-2026-28684 (symlink in set_key)
