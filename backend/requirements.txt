# Hustlrzz backend dependencies (minimal, unpinned on purpose).
#
# Every package here is imported somewhere under backend/ and each ships
# prebuilt wheels for Python 3.10+. Versions are floors, so pip picks a
# release with wheels for whatever Python it runs on (local, Render, Docker).

# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
pydantic[email]>=2.7
email-validator>=2.0

# --- Firebase (auth + Firestore) ---
firebase-admin>=6.5

# --- HTTP clients ---
httpx>=0.27
requests>=2.31
PyJWT>=2.8

# --- PDF resume parsing ---
PyMuPDF>=1.24

# --- Web search & scraping ---
beautifulsoup4>=4.12
lxml>=5.0
playwright>=1.40
duckduckgo-search>=8.0

# --- Misc ---
python-dotenv>=1.0
